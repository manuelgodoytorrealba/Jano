const assert=require('node:assert/strict'),fs=require('node:fs');const rows=JSON.parse(fs.readFileSync('/tmp/w3rows.json'));assert.equal(rows.length,22);assert.equal(rows.filter(r=>r.preparedChars>=160).length,21);const semantic=JSON.parse(fs.readFileSync('artifacts/feed-wave-003-semantic.json'));assert.equal(semantic.rows.length,3);assert.equal(semantic.rows.every(r=>r.result.decision==='REVIEW'),true);console.log('PASS wave003 zero-yield fixture');
